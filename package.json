{
  "name": "company-url-guesser",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.16.2",
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "dev": "react-scripts start & node server.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "prestart": "npm run build",
    "start": "node server.js"
  },
  "devDependencies": {
    "cors": "^2.8.4"
  }
}
